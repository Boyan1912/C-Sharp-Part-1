namespace Problem6QuadraticEquation
{
    using System;
    class Program
    {
        static void Main()
        {
            //The solutions of the quadratic equation ax2 + bx + c = 0 correspond to the roots of the function f(x) = ax2 + bx + c, since they are the values of x for which f(x) = 0. (Wikipedia)


            double a = double.Parse(Console.ReadLine());
            double c = double.Parse(Console.ReadLine());
            double b = double.Parse(Console.ReadLine());

            // http://www.sanfoundry.com/csharp-program-roots-quadratic-equation/

            // Нищо не разбрах от тази задача :)
        }
    }
}
